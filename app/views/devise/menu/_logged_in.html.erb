<% if user_signed_in? %>

<li><%=link_to current_user.username, username_path(current_user.username.downcase)%>
	<%if new_message?%>
	<span class='newMessage'><%=message_received%><span>
		<%end%>	
	</li>
	<li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>

	<% end %>